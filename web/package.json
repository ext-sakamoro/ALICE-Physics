{
  "name": "alice-physics-web",
  "version": "0.3.0",
  "description": "ALICE-Physics WebAssembly 3D Viewer",
  "private": true,
  "scripts": {
    "build:wasm": "cd .. && wasm-pack build --target web --out-dir web/pkg -- --features wasm",
    "dev": "vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}

# Determinism gate (v1.1.0): the platform `libm` is not bit-exact across
# OS / CPU / compiler, so every float transcendental must go through
# `alice_physics::det_math`. CI runs clippy with `-D warnings`, which turns
# `disallowed_methods` into a red gate. `Fix128::sin` etc. are unaffected
# (different receiver type).
disallowed-methods = [
    { path = "f32::sin", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::cos", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::tan", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::asin", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::acos", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::atan", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::atan2", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::sinh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::cosh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::tanh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::asinh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::acosh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::atanh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::exp", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::exp2", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::exp_m1", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::ln", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::ln_1p", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::log", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::log2", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::log10", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::powf", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::powi", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::cbrt", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f32::hypot", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::sin", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::cos", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::tan", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::asin", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::acos", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::atan", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::atan2", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::sinh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::cosh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::tanh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::asinh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::acosh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::atanh", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::exp", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::exp2", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::exp_m1", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::ln", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::ln_1p", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::log", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::log2", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::log10", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::powf", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::powi", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::cbrt", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
    { path = "f64::hypot", reason = "platform libm is not cross-platform bit-exact; use alice_physics::det_math" },
]
